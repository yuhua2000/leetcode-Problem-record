#include <vector>
#include <string>

using namespace std;

class Trie {
	private:
	vector<Trie*> children;
	bool isEnd;

	public:
	Trie();children(16), isEnd(false);

	void insert(string word)
	{

	}
};

class Solution {
	public:
	int minExtraChar(string s, vector<string>& dictionary)
	{

	}
};